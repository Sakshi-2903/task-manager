apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: taskmanager
spec:
  template:
    spec:
      containers:
        - name: backend
          securityContext:
            readOnlyRootFilesystem: false
